### Generated by Eclipse Vorto OpenAPI Generator from Model 'vorto.private.local.com.bosch.sh:UniversalSwitch:1.0.0'
openapi: 3.0.0
info:
  title: Bosch Smart Home Local API for Universal Switch
  description: |-
    This description focuses on the JSON-based, REST-like API for a Universal Switch.
    
    By using this documentation, the developer accepts and agrees to be bound by our [Terms and Conditions](https://github.com/BoschSmartHome/bosch-shc-api-docs#terms-and-conditions).
    
    This documentation is subject to the [Creative Commons Attribution-NonCommercial-NoDerivatives 4.0 International Public License](https://creativecommons.org/licenses/by-nc-nd/4.0/legalcode).
    
    Please report any issues you encounter with this documentation in our [GitHub tracker](https://github.com/BoschSmartHome/bosch-shc-api-docs/issues).
  version: "1.0"
servers:
  - url: https://{shcIp}:8444/smarthome
    description: "Bosch Smart Home Controller URL"
    variables:
      shcIp:
        default: 192.168.0.10
tags:
  - name: Services
    description: Services of your UniversalSwitch
  - name: States
    description: States of your UniversalSwitch
paths:
  '/devices/{deviceId}/services/Keypad':
    get:
      summary: Retrieve the Keypad service of the UniversalSwitch.
      description: |-
        Control of up to four commands without the need of your smartphone. Activate or deactivate lighting and alarm settings at the touch of a button when you enter your home or enter a room. Can be used flexibly in your home and is always within reach.
      tags:
        - Services
      parameters:
        - $ref: '#/components/parameters/apiVersionHeaderParam'
        - $ref: '#/components/parameters/deviceIdPathParam'
      responses:
        '200':
          description: The Keypad was successfully retrieved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KeypadService'
        '404':
          description: |-
            The entity could not be found. One of the defined query parameters was invalid.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdvancedError'
  '/devices/{deviceId}/services/Keypad/state':
    get:
      summary: Retrieve the state of the Keypad service.
      description: |-
        Retrieve the state of the Keypad service identified by the
        `deviceId` path parameter.
      tags:
        - States
      parameters:
        - $ref: '#/components/parameters/apiVersionHeaderParam'
        - $ref: '#/components/parameters/deviceIdPathParam'
      responses:
        '200':
          description: The state of Keypad was successfully retrieved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KeypadServiceStates'
        '404':
          description: |-
            The entity could not be found. One of the defined query parameters was invalid.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdvancedError'

components:
  schemas:
    AdvancedError:
      type: object
      properties:
        '@type':
          type: string
          description: The type of the Object.
          example: JsonRestExceptionResponseEntity
        errorCode:
          type: string
          description: The error code of the occurred Exception.
        statusCode:
          type: integer
          description: The HTTP status of the error.
    ServiceDefinition:
      type: array
      minItems: 1
      uniqueItems: true
      items:
        type: string
        description: "A single fully qualified identifier of the Service of a Device."
      example:
        - Keypad
    KeypadServiceStates:
      type: object
      description: KeypadService states of UniversalSwitch
      properties:
        keyCode:
          type: integer
        keyName:
          $ref: '#/components/schemas/KeypadKeyState'
        eventType:
          $ref: '#/components/schemas/KeypadEventState'
        eventTimestamp:
          type: integer
    KeypadService:
      type: object
      properties:
        '@type':
          type: string
          enum: [DeviceServiceData]
          description: The type of the Object.
        id:
          type: string
          enum: [deviceServiceId]
          description: "A single fully qualified identifier of the Service of a Device."
        deviceId:
          type: string
          description: "A single fully qualified identifier of the Device."
        state:
          $ref: '#/components/schemas/KeypadServiceStates'
        path:
          type: string
          description: The path to the Property.
    KeypadKeyState:
      type: string
      enum: [LOWER_BUTTON,UPPER_BUTTON, LOWER_LEFT_BUTTON, LOWER_RIGHT_BUTTON, UPPER_LEFT_BUTTON, UPPER_RIGHT_BUTTON]
    KeypadEventState:
      type: string
      enum: [PRESS_SHORT,PRESS_LONG]

  parameters:
    apiVersionHeaderParam:
      in: header
      name: api-version
      description: The version of the API.
      schema:
        type: string
        example: "1.0"
    deviceIdPathParam:
      name: deviceId
      in: path
      description: "A single fully qualified identifier of the Device."
      required: true
      schema:
        type: string
    propertyPathPathParam:
      name: propertyPath
      in: path
      description: The path to the Property.
      required: true
      schema:
        type: string