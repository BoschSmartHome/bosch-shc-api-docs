### Generated by Eclipse Vorto OpenAPI Generator from Model 'vorto.private.local.com.bosch.sh:SHC_Info:1.0.0'
openapi: 3.0.0
info:
  title: Bosch Smart Home Local API for SHC Information
  description: |-
    This description focuses on the JSON-based, REST-like API for retrieving Smart Home Controller Information.

    By using this documentation, the developer accepts and agrees to be bound by our [Terms and Conditions](https://github.com/BoschSmartHome/bosch-shc-api-docs#terms-and-conditions).

    This documentation is subject to the [Creative Commons Attribution-NonCommercial-NoDerivatives 4.0 International Public License](https://creativecommons.org/licenses/by-nc-nd/4.0/legalcode).

    Please report any issues you encounter with this documentation in our [GitHub tracker](https://github.com/BoschSmartHome/bosch-shc-api-docs/issues).
  version: "1.0"
servers:
  - url: https://{shcIp}:8446/public
    description: "Bosch Smart Home Controller Public Info URL"
    variables:
      shcIp:
        default: 192.168.0.10
tags:
  - name: SHC Info
    description: Information about the Smart Home Controller
paths:
  '/information':
    get:
      summary: Get Smart Home Controller generation info.
      description: Returns information about the Smart Home Controller, including its generation.
      tags:
        - SHC Info
      parameters: []
      responses:
        '200':
          description: SHC information successfully retrieved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SHCInformation'
        '404':
          description: The entity could not be found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdvancedError'

components:
  schemas:
    AdvancedError:
      type: object
      properties:
        '@type':
          type: string
          description: The type of the Object.
          example: JsonRestExceptionResponseEntity
        errorCode:
          type: string
          description: The error code of the occurred Exception.
        statusCode:
          type: integer
          description: The HTTP status of the error.
    SHCInformation:
      type: object
      properties:
        apiVersions:
          type: array
          items:
            type: string
        softwareUpdateState:
          type: object
          properties:
            '@type':
              type: string
              enum: [softwareUpdateState]
            swUpdateState:
              type: string
              enum: [NO_UPDATE_AVAILABLE, UPDATE_AVAILABLE, DOWNLOADING, INSTALLING, UPDATE_SUCCESS, UPDATE_FAILED] # Example enums, adjust as needed
            swUpdateLastResult:
              type: string
              enum: [UPDATE_SUCCESS, UPDATE_FAILED, UNKNOWN] # Example enums
            swUpdateAvailableVersion:
              type: string
            swInstalledVersion:
              type: string
            swActivationDate:
              type: object
              properties:
                '@type':
                  type: string
                  enum: [softwareActivationDate]
                timeout:
                  type: integer
                  format: int64
            automaticUpdatesEnabled:
              type: boolean
        shcGeneration:
          type: string
          enum: [SHC_1, SHC_2]
          description: The generation of the Smart Home Controller.
      required:
        - apiVersions
        - softwareUpdateState
        - shcGeneration
  parameters:
    apiVersionHeaderParam:
      in: header
      name: api-version
      description: The version of the API.
      schema:
        type: string
        example: "1.0"