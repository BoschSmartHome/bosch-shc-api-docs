### Generated by Eclipse Vorto OpenAPI Generator from Model 'vorto.private.local.com.bosch.sh:ClimateControl:1.0.0'
openapi: 3.0.0
info:
  title: Bosch Smart Home Local API for Climate Control
  description: |-
    This description focuses on the JSON-based, REST-like API for a Climate Control.
    
    By using this documentation, the developer accepts and agrees to be bound by our [Terms and Conditions](https://github.com/BoschSmartHome/bosch-shc-api-docs#terms-and-conditions).
    
    This documentation is subject to the [Creative Commons Attribution-NonCommercial-NoDerivatives 4.0 International Public License](https://creativecommons.org/licenses/by-nc-nd/4.0/legalcode).
    
    Please report any issues you encounter with this documentation in our [GitHub tracker](https://github.com/BoschSmartHome/bosch-shc-api-docs/issues).
  version: "1.0"
servers:
  - url: https://{shcIp}:8444/smarthome
    description: "Bosch Smart Home Controller URL"
    variables:
      shcIp:
        default: 192.168.0.10
tags:
  - name: Services
    description: Services of your ClimateControl
  - name: States
    description: States of your ClimateControl
paths:
  '/devices/{deviceId}/services/RoomClimateControl':
    get:
      summary: Retrieve the RoomClimateControl service of the ClimateControl.
      description: |-
        Control of up to six Bosch Smart Home radiator thermostats per room with this virtual device.
      tags:
        - Services
      parameters:
        - $ref: '#/components/parameters/apiVersionHeaderParam'
        - $ref: '#/components/parameters/deviceIdPathParam'
      responses:
        '200':
          description: The RoomClimateControl was successfully retrieved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoomClimateControlService'
        '404':
          description: |-
            The entity could not be found. One of the defined query parameters was invalid.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdvancedError'
  '/devices/{deviceId}/services/RoomClimateControl/state':
    get:
      summary: Retrieve the state of the RoomClimateControl service.
      description: |-
        Retrieve the state of the RoomClimateControl service identified by the
        `deviceId` path parameter.
      tags:
        - States
      parameters:
        - $ref: '#/components/parameters/apiVersionHeaderParam'
        - $ref: '#/components/parameters/deviceIdPathParam'
      responses:
        '200':
          description: The state of RoomClimateControl was successfully retrieved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoomClimateControlServiceStates'
        '404':
          description: |-
            The entity could not be found. One of the defined query parameters was invalid.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdvancedError'
    put:
      summary: Executes the climateControlState on the device.
      description: |-
        Executes the climateControlState on the device identified by the `deviceId` path parameter.
      tags:
        - States
      parameters:
        - $ref: '#/components/parameters/apiVersionHeaderParam'
        - $ref: '#/components/parameters/deviceIdPathParam'
      responses:
        '204':
          description: Accepted request.
        '400':
          description: One of the defined query parameters was invalid.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdvancedError'
        '404':
          description: The entity could not be found. One of the defined path parameters was invalid.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdvancedError'
        '405':
          description: The method was not allowed.
        '422':
          description: Mapping of defined query parameter failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdvancedError'
      requestBody:
        $ref: '#/components/requestBodies/RoomClimateControlServiceClimateControlStatePayload'
  '/devices/{deviceId}/services/TemperatureLevel':
    get:
      summary: Retrieve the TemperatureLevel service of the ClimateControl.
      description: |-
        Measures the temperature at a central point in the room.
      tags:
        - Services
      parameters:
        - $ref: '#/components/parameters/apiVersionHeaderParam'
        - $ref: '#/components/parameters/deviceIdPathParam'
      responses:
        '200':
          description: The TemperatureLevel was successfully retrieved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TemperatureLevelService'
        '404':
          description: |-
            The entity could not be found. One of the defined query parameters was invalid.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdvancedError'
  '/devices/{deviceId}/services/TemperatureLevel/state':
    get:
      summary: Retrieve the state of the TemperatureLevel service.
      description: |-
        Retrieve the state of the TemperatureLevel service identified by the
        `deviceId` path parameter.
      tags:
        - States
      parameters:
        - $ref: '#/components/parameters/apiVersionHeaderParam'
        - $ref: '#/components/parameters/deviceIdPathParam'
      responses:
        '200':
          description: The state of TemperatureLevel was successfully retrieved.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TemperatureLevelServiceStates'
        '404':
          description: |-
            The entity could not be found. One of the defined query parameters was invalid.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdvancedError'

components:
  schemas:
    AdvancedError:
      type: object
      properties:
        '@type':
          type: string
          description: The type of the Object.
          example: JsonRestExceptionResponseEntity
        errorCode:
          type: string
          description: The error code of the occurred Exception.
        statusCode:
          type: integer
          description: The HTTP status of the error.
    ServiceDefinition:
      type: array
      minItems: 1
      uniqueItems: true
      items:
        type: string
        description: "A single fully qualified identifier of the Service of a Device."
      example:
        - RoomClimateControl
        - TemperatureLevel
    RoomClimateControlServiceStates:
      type: object
      description: RoomClimateControlService states of ClimateControl
      properties:
        '@type':
          type: string
          enum: [climateControlState]
          description: The type of the Object.
        setpointTemperature:
          $ref: '#/components/schemas/SetpointTemperatureState'
    RoomClimateControlService:
      type: object
      properties:
        '@type':
          type: string
          enum: [DeviceServiceData]
          description: The type of the Object.
        id:
          type: string
          enum: [deviceServiceId]
          description: "A single fully qualified identifier of the Service of a Device."
        deviceId:
          type: string
          description: "A single fully qualified identifier of the Device."
        state:
          $ref: '#/components/schemas/RoomClimateControlServiceStates'
        path:
          type: string
          description: The path to the Property.
    TemperatureLevelServiceStates:
      type: object
      description: TemperatureLevelService states of ClimateControl
      properties:
        temperature:
          type: number
    TemperatureLevelService:
      type: object
      properties:
        '@type':
          type: string
          enum: [DeviceServiceData]
          description: The type of the Object.
        id:
          type: string
          enum: [deviceServiceId]
          description: "A single fully qualified identifier of the Service of a Device."
        deviceId:
          type: string
          description: "A single fully qualified identifier of the Device."
        state:
          $ref: '#/components/schemas/TemperatureLevelServiceStates'
        path:
          type: string
          description: The path to the Property.
    SetpointTemperatureState:
      type: string
      enum: ["5.0", "5.5", "...", "29.5", "30.0"]
  
  requestBodies:
    RoomClimateControlServiceClimateControlStatePayload:
      content:
        application/json:
          schema:
            type: object
            properties:
              '@type':
                type: string
                enum: [climateControlState]
                description: The type of the Object.
              setpointTemperature:
                $ref: '#/components/schemas/SetpointTemperatureState'
  parameters:
    apiVersionHeaderParam:
      in: header
      name: api-version
      description: The version of the API.
      schema:
        type: string
        example: "1.0"
    deviceIdPathParam:
      name: deviceId
      in: path
      description: "A single fully qualified identifier of the Device."
      required: true
      schema:
        type: string
    propertyPathPathParam:
      name: propertyPath
      in: path
      description: The path to the Property.
      required: true
      schema:
        type: string